
#include <bits/stdc++.h>

using namespace std;

#define maxn int(3e3 + 12)

int n, a[maxn], b[maxn], c[maxn];
int dp[maxn];

int main()
{
	scanf("%d", &n);
	for (int i = 1; i <= n; i++)
		scanf("%d", a + i);
	for (int i = 1; i <= n; i++)
		scanf("%d", b + i);
	for (int i = 1; i <= n; i++)
		scanf("%d", c + i);
	
}